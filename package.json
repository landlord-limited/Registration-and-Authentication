{
  "name": "registration-authentication-app",
  "version": "1.0.0",
  "description": "A React app for registration and authentication with land tokenization.",
  "main": "index.js",
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "dependencies": {
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "react-scripts": "4.0.3",
    "react-router-dom": "^5.3.0"
    // Add other dependencies as needed
  },
  "devDependencies": {},
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  }
}
